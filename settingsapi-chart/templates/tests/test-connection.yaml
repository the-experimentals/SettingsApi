apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "settingsapi-chart.fullname" . }}-test-connection"
  labels:
    {{- include "settingsapi-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "settingsapi-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
